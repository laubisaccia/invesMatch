<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/invesmatch_logo"
        android:label="InvesMatch"
        android:roundIcon="@drawable/invesmatch_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.InvesMatch"
        tools:targetApi="31" >
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.Splashscreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--Agregamos para mostrar el back -->
        <activity
            android:name=".InvestorProfileActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">

        </activity>
        <activity
            android:name=".UserProfileActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">
        </activity>
        <activity
            android:name=".InvestmentComparisonActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">
        </activity>
        <activity
            android:name=".TermsAndConditionsActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">
        </activity>
        <activity
            android:name=".HistoryActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">
        </activity>
        <activity
            android:name=".ResultActivity"
            android:theme="@style/Theme.InvesMatch.DarkActionBar">
        </activity>


    </application>

</manifest>